# neural-core
- Train custom language models on user-provided text - Continuous learning from new data without full retraining - Token-based text generation with configurable parameters - Corpus statistics and training state persistence
